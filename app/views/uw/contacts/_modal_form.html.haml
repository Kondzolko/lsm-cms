.modal{:style => "display:block", :tabindex => "-1"}
  .modal-dialog
    .modal-content
      .modal-header
        %h5.modal-title Coverholder contact
        %button.close{"data-reflex" => "click->ContactReflex#default_reflex", :type => "button"}
          %span ×
      .modal-body
        = simple_form_for [:uw, :coverholder, contact], url: '#', html: { data: { reflex: "submit->ContactReflex#submit", id: contact.id, coverholder_id: contact.coverholder_id } } do |f|
          = f.input :name
          = f.input :email
          = f.submit "Save", class: "btn btn-primary"
          %button.btn.btn-link{"data-reflex" => "click->ContactReflex#default_reflex", :type => "button"} Cancel
